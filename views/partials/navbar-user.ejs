<nav id="user-nav" class="fixed w-full top-0 z-[100] transition-all duration-500 bg-[#0a0a0a]/95 backdrop-blur-md border-b border-white/10">
    <div class="max-w-7xl mx-auto px-6">
        <div class="flex items-center justify-between h-20 transition-all duration-300" id="nav-container">
            
            <a href="/dashboard" class="flex items-center gap-4 group cursor-pointer">
                <div class="relative">
                    <img src="/images/logo1.png" alt="RIT Logo" 
                         class="h-10 md:h-12 w-auto object-contain z-10 relative">
                    <div class="absolute inset-0 bg-white/10 blur-xl rounded-full scale-50 group-hover:scale-100 transition-transform duration-700"></div>
                </div>

                <div class="flex flex-col">
                    <div class="flex items-baseline">
                        <span class="text-xl md:text-2xl font-black text-white tracking-tighter uppercase italic">RIT</span>
                        <span class="text-xl md:text-2xl font-extralight tracking-tight ml-1.5 bg-gradient-to-r from-gray-100 via-white to-gray-500 bg-clip-text text-transparent">
                            Acade<span class="font-bold">MI</span>a
                        </span>
                    </div>
                </div>
            </a>

            <div class="hidden md:flex items-center gap-8">
                <div class="flex items-center gap-6 text-[12px] font-bold uppercase tracking-[0.2em] text-gray-400">
                    <a href="/dashboard" class="user-nav-link hover:text-white transition-colors">Dashboard</a>
                    <a href="/subjects" class="user-nav-link hover:text-white transition-colors">Subjects</a>
                    <a href="/profile" class="user-nav-link hover:text-white transition-colors">Profile</a>
                </div>
                
                <div class="h-6 w-[1px] bg-white/10 mx-2"></div>
                
                <a href="/auth/logout" 
                   class="group flex items-center gap-2 bg-white/5 border border-white/10 px-4 py-2 rounded-xl text-[11px] font-black uppercase tracking-widest text-red-400 hover:bg-red-500 hover:text-white hover:border-red-500 transition-all duration-300">
                   <span>Logout</span>
                   <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" /></svg>
                </a>
            </div>

            <button id="menu-btn-user" class="md:hidden p-2 text-white focus:outline-none">
                <div id="menu-icon-user" class="w-6 h-4 relative flex flex-col justify-between">
                    <span class="w-full h-[2px] bg-white rounded-full transition-all duration-300"></span>
                    <span class="w-3/4 h-[2px] bg-white rounded-full transition-all duration-300 self-end"></span>
                    <span class="w-full h-[2px] bg-white rounded-full transition-all duration-300"></span>
                </div>
            </button>
        </div>
    </div>

    <div id="mobile-menu-user" class="hidden md:hidden bg-[#0a0a0a] border-t border-white/10 absolute w-full left-0 top-full shadow-2xl animate-fade-in">
        <div class="flex flex-col p-8 gap-6 text-center font-bold uppercase tracking-widest text-sm">
            <a href="/dashboard" class="text-gray-400 hover:text-white">Dashboard</a>
            <a href="/subjects" class="text-gray-400 hover:text-white">Subjects</a>
            <a href="/profile" class="text-gray-400 hover:text-white">Profile</a>
            <hr class="border-white/5">
            <a href="/auth/logout" class="text-red-400">Logout System</a>
        </div>
    </div>
</nav>

<style>
    /* Desktop Underline Effect */
    .user-nav-link {
        position: relative;
    }
    .user-nav-link::after {
        content: '';
        position: absolute;
        width: 0;
        height: 2px;
        bottom: -6px;
        left: 0;
        background: #fff;
        transition: width 0.3s ease;
    }
    .user-nav-link:hover::after {
        width: 100%;
    }

    /* Shrink Effect on Scroll */
    .nav-shrink-user {
        background: rgba(10, 10, 10, 0.98);
    }
    .nav-shrink-user #nav-container {
        height: 70px;
    }

    /* Mobile Hamburger Animation */
    .menu-open-user span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
    .menu-open-user span:nth-child(2) { opacity: 0; transform: translateX(10px); }
    .menu-open-user span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }

    @keyframes fade-in {
        from { opacity: 0; transform: translateY(-10px); }
        to { opacity: 1; transform: translateY(0); }
    }
    .animate-fade-in { animation: fade-in 0.3s ease-out forwards; }
</style>

<script>
    const navUser = document.getElementById('user-nav');
    const btnUser = document.getElementById("menu-btn-user");
    const menuUser = document.getElementById("mobile-menu-user");
    const iconUser = document.getElementById("menu-icon-user");

    // Scroll Effect
    window.addEventListener('scroll', () => {
        if (window.scrollY > 20) {
            navUser.classList.add('nav-shrink-user', 'shadow-2xl');
        } else {
            navUser.classList.remove('nav-shrink-user', 'shadow-2xl');
        }
    });

    // Mobile Toggle
    btnUser.addEventListener("click", () => {
        menuUser.classList.toggle("hidden");
        iconUser.classList.toggle("menu-open-user");
    });
</script>