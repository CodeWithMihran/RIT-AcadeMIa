<nav id="main-nav"
    class="fixed w-full top-0 z-[100] transition-all duration-500 bg-[#0a0a0a]/90 backdrop-blur-md border-b border-white/10">
    <div class="max-w-7xl mx-auto px-6">
        <div class="flex items-center justify-between h-20 transition-all duration-300" id="nav-container">

            <div class="flex items-center gap-5 group cursor-pointer">
                <div class="relative">
                    <img src="/images/logo1.png" alt="RIT Logo"
                        class="h-12 md:h-15 w-auto object-contain z-10 relative">
                    <div
                        class="absolute inset-0 bg-white/10 blur-xl rounded-full scale-50 group-hover:scale-100 transition-transform duration-700">
                    </div>
                </div>

                <div class="flex flex-col">
                    <div class="flex items-baseline">
                        <span class="text-2xl md:text-3xl font-black text-white tracking-tighter uppercase italic">
                            RIT
                        </span>
                        <span
                            class="text-2xl md:text-3xl font-extralight tracking-tight ml-2 bg-gradient-to-r from-gray-100 via-white to-gray-500 bg-clip-text text-transparent">
                            Acade<span class="font-bold">MI</span>a
                        </span>
                    </div>
                    <div
                        class="h-[2px] w-0 group-hover:w-full bg-gradient-to-r from-blue-500 to-transparent transition-all duration-500 mt-0.5">
                    </div>
                </div>
            </div>

            <div class="hidden md:flex items-center gap-10">
                <div class="flex items-center gap-8 text-[13px] font-semibold uppercase tracking-widest text-gray-400">
                    <a href="#home" class="nav-link hover:text-white transition-colors">Home</a>
                    <a href="#features" class="nav-link hover:text-white transition-colors">Features</a>
                    <a href="#workflow" class="nav-link hover:text-white transition-colors">Workflow</a>
                </div>

                <div class="flex items-center gap-5 ml-4">
                    <a href="#auth" class="text-[14px] font-medium text-white/70 hover:text-white transition-colors">Log
                        in</a>
                    <a href="#auth"
                        class="bg-white text-black px-6 py-2.5 rounded-full text-[14px] font-bold hover:bg-gray-200 transition-all transform hover:scale-105 active:scale-95 shadow-[0_0_20px_rgba(255,255,255,0.1)]">
                        Get Started
                    </a>
                </div>
            </div>

            <button id="menu-btn" class="md:hidden p-2 text-white focus:outline-none">
                <div id="menu-icon" class="w-6 h-4 relative flex flex-col justify-between">
                    <span class="w-full h-[2px] bg-white rounded-full transition-all duration-300"></span>
                    <span class="w-3/4 h-[2px] bg-white rounded-full transition-all duration-300 self-end"></span>
                    <span class="w-full h-[2px] bg-white rounded-full transition-all duration-300"></span>
                </div>
            </button>
        </div>
    </div>

    <div id="mobile-menu"
        class="hidden md:hidden bg-[#0a0a0a] border-t border-white/10 absolute w-full left-0 top-full animate-fade-in-down">
        <div class="flex flex-col p-8 gap-6 text-center font-medium">
            <a href="#home" class="mobile-link text-gray-400 hover:text-white">Home</a>
            <a href="#features" class="mobile-link text-gray-400 hover:text-white">Features</a>
            <a href="#workflow" class="mobile-link text-gray-400 hover:text-white">Workflow</a>
            <div class="h-[1px] bg-white/10 w-full"></div>
            <a href="#auth" class="bg-white text-black py-3 rounded-xl font-bold">Start Learning</a>
        </div>
    </div>
</nav>

<style>
    /* Premium Hover Underline */
    .nav-link {
        position: relative;
    }

    .nav-link::after {
        content: '';
        position: absolute;
        width: 0;
        height: 2px;
        bottom: -6px;
        left: 0;
        background: linear-gradient(90deg, #fff, transparent);
        transition: width 0.3s ease;
    }

    .nav-link:hover::after {
        width: 100%;
    }

    /* Navbar Shrink on Scroll */
    .nav-shrink {
        background: rgba(10, 10, 10, 0.98);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .nav-shrink #nav-container {
        height: 65px;
    }

    /* Hamburger Animation */
    .menu-open span:nth-child(1) {
        transform: translateY(7px) rotate(45deg);
    }

    .menu-open span:nth-child(2) {
        opacity: 0;
        transform: translateX(10px);
    }

    .menu-open span:nth-child(3) {
        transform: translateY(-7px) rotate(-45deg);
    }

    @keyframes fade-in-down {
        from {
            opacity: 0;
            transform: translateY(-10px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .animate-fade-in-down {
        animation: fade-in-down 0.3s ease-out forwards;
    }
</style>

<script>
    const nav = document.getElementById('main-nav');
    const btn = document.getElementById("menu-btn");
    const menu = document.getElementById("mobile-menu");
    const icon = document.getElementById("menu-icon");

    // Scroll Logic
    window.addEventListener('scroll', () => {
        if (window.scrollY > 50) {
            nav.classList.add('nav-shrink', 'shadow-2xl');
        } else {
            nav.classList.remove('nav-shrink', 'shadow-2xl');
        }
    });

    // Mobile Toggle
    btn.addEventListener("click", () => {
        menu.classList.toggle("hidden");
        icon.classList.toggle("menu-open");
    });
</script>